
# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample Tasks page
GET           /tasks            controllers.TasksController.index
POST          /tasks            controllers.TasksController.store
PUT           /tasks/:id        controllers.TasksController.update(id: Long)
DELETE        /tasks/:id        controllers.TasksController.destroy(id: Long)
